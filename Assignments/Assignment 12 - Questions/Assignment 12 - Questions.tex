\documentclass[11pt, a4, twoside]{article}

\usepackage{
    amsmath,
    amsfonts,
    amssymb
}

\usepackage{subcaption}
\usepackage{xcolor}
\usepackage{
	tikz,
	pgfplots
}


\ExplSyntaxOn
\cs_set_eq:NN \etex_iffontchar:D \tex_iffontchar:D
\ExplSyntaxOff

\usepackage{mathtools}
\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=[rgb]{0,0,0.6953125},
	filecolor=[rgb]{0,0,0.6953125},      
	urlcolor=[rgb]{0,0,0.6953125},
	citecolor=[rgb]{0,0,0.6953125}
}
\newcommand{\hint}[1]{
	\begin{flushleft}
		\hyperlink{#1}{راهنمایی}
	\end{flushleft}
}
\usepackage{UTProblemSet}
\DefaultMathDigits

\begin{document}
			%%%%%%%%%%%%%%%%%%% سربرگ سند %%%%%%%%%%%%%%%%%%%
	\pagestyle{empty}
	\heading
	{فرآیندهای تصادفی}
	{دکتر صفری}
	{نیمسال دوم 1403-1404}
    {سه‌شنبه، 20 خرداد 23:59}
    {تمرین سری دوازدهم}
	\inspiringQuotation
	{\\We have to remember that what we observe is not nature in itself but nature exposed to our method of questioning.}
	{Werner Heisenberg (1901-1976)}
			%%%%%%%%%%%%%%%%%%% بخش سوالات %%%%%%%%%%%%%%%%%%%
	\begin{problem}
		یک فرایند زاد و مرگ را روی اعداد صحیح نامنفی در نظر بگیرید که نرخ‌های مرگ آن به صورت $\mu_x = x,\ x \geq 0$ داده شده‌اند. تعیین کنید که آیا این فرایند گذرا \LTRfootnote{transient}، بازگشت‌پذیر خنثی \LTRfootnote{null recurrent} یا بازگشت‌پذیر مثبت \LTRfootnote{positive recurrent} است اگر نرخ‌های تولد به صورت زیر باشند:

		\begin{itemize}
		\item[(آ)] $\lambda_x = x + 1,\ x \geq 0;$
		\item[(ب)] $\lambda_x = x + 2,\ x \geq 0.$
		\end{itemize}
	\end{problem}

	\begin{problem}
		فرض کنید $Y$ یک متغیر تصادفی نمایی با نرخ $\lambda$ باشد که مستقل از زنجیره مارکوف زمان‌پیوسته $\{X(t)\}$ است، و تعریف کنید:
		\[
		\bar{P}_{ij} = P(X(Y) = j \mid X(0) = i)
		\]

		\begin{itemize}
		\item[(آ)] نشان دهید که:
		\[
		\bar{P}_{ij} = \frac{1}{v_i + \lambda} \sum_k q_{ik} \bar{P}_{kj} + \frac{\lambda}{v_i + \lambda} \delta_{ij}
		\]
		که در آن $\delta_{ij} = 1$ اگر $i = j$ و صفر اگر $i \ne j$ باشد.

		\item[(ب)] نشان دهید که پاسخ دستگاه معادلات بالا به صورت زیر داده می‌شود:
		\[
		\bar{\mathbf{P}} = (\mathbf{I} - \mathbf{R}/\lambda)^{-1}
		\]
		که در آن $\bar{\mathbf{P}}$ ماتریسی از درایه‌های $\bar{P}_{ij}$ است، $\mathbf{I}$ ماتریس همانی است، و $\mathbf{R}$ ماتریس مولد است.

		\item[(ج)] فرض کنید اکنون که $Y_1, \ldots, Y_n$ متغیرهای تصادفی مستقل نمایی با نرخ $\lambda$ هستند که مستقل از $\{X(t)\}$ می‌باشند. نشان دهید که:
		\[
		P(X(Y_1 + \cdots + Y_n) = j \mid X(0) = i)
		\]
		برابر است با درایه‌ی سطر $i$ و ستون $j$ از ماتریس $\bar{\mathbf{P}}^n$.
		\end{itemize}
	\end{problem}

	\begin{problem}
		فرض کنید \(\{X(t), t \ge 0\}\) یک زنجیره مارکوف زمان پیوسته با فضای حالت \(S = \{0,1,2\}\) و مولد زیر باشد
		\[		q = \begin{bmatrix}
			-2 & 1 & 1 \\
			2 & -3 & 1 \\
			1 & 2 & -3
		\end{bmatrix}
		\]				

		مقادیر زیر را تعیین کنید:
		\begin{itemize}
			\item[(آ)]  توزیع زمان‌ باقی ماندن در وضعیت 1.
			\item[(ب)]  ماتریس انتقال Q از زنجیره تعبیه شده.
			\item[(پ)]  \(P(\tau_2 \mid X(\tau_1) = 1)\) که در آن \(\tau_{i}\) زمان گذار i ام است و \(X(0) = 0\).
		\end{itemize}
	\end{problem}

	\begin{problem}
		 مشتریان طبق یک فرایند پواسون با نرخ $\lambda$ به یک ایستگاه خدماتی دو سروره می‌رسند. هر زمان که مشتری جدیدی وارد می‌شود، هر مشتری که در سیستم است فوراً خارج می‌شود. یک ورودی جدید ابتدا با سرور 1 و سپس با سرور 2 وارد سرویس می‌شود. اگر زمان‌های سرویس در سرورها به صورت مستقل نمایی با نرخ‌های مربوطه $\mu_1$ و $\mu_2$ باشند، چه نسبتی از مشتریان ورودی سرویس خود را با سرور 2 تکمیل می‌کنند؟ (یعنی چه نسبتی از مشتریان ورودی هر دو سرویس را بدون مانع تکمیل می‌کنند.)
	\end{problem}
	
	\begin{problem}
	یک فرایند تولد و مرگ بازگشتی مثبت و تحویل‌ناپذیر را روی $\mathcal{S} = \{0, 1, 2, \ldots\}$ در نظر بگیرید، و فرض کنید $X(0)$ توزیع ایستا $\pi$ را برای توزیع اولیه خود دارد. سپس $X(t)$ برای تمام $t \ge 0$ توزیع $\pi$ را خواهد داشت. کمیت‌های
	\[ E[\lambda_{X(t)}] = \sum_{x=0}^{\infty} \lambda_x \pi(x) \quad \text{و} \quad E[\mu_{X(t)}] = \sum_{x=0}^{\infty} \mu_x \pi(x) \]
	می‌توانند به ترتیب به عنوان نرخ متوسط تولد و نرخ متوسط مرگ فرایند تفسیر شوند.
		\begin{itemize}
			\item[(آ)] نشان دهید که نرخ متوسط تولد برابر با نرخ متوسط مرگ است.
			\item[(ب)] بخش (آ) چه مفهومی برای یک صف $N$ سرور بازگشتی مثبت دارد؟
		\end{itemize}
	\end{problem}


	% \begin{problem}
	% 	یک سیستم متشکل از $N$ ماشین با زمان‌های توزیع نمایی با نرخ $\lambda$ کار می‌کند تا از کار بیفتد. پس از خرابی، یک ماشین باید از دو فاز سرویس عبور کند. فاز 1 سرویس برای یک زمان نمایی با نرخ $\mu$ طول می‌کشد، و همیشه \textbf{سرورهایی} برای سرویس فاز 1 در دسترس هستند. پس از اتمام فاز 1 سرویس، ماشین به یک \textbf{سرور} می‌رود که سرویس فاز 2 را انجام می‌دهد. اگر آن \textbf{سرور} مشغول باشد، ماشین به صف انتظار می‌پیوندد. زمان لازم برای تکمیل سرویس فاز 2 نمایی با نرخ $\nu$ است. پس از تکمیل سرویس فاز 2، ماشین به کار بازمی‌گردد. زنجیره مارکوف زمان پیوسته را در نظر بگیرید که حالت آن در هر زمان سه تایی از اعداد نامنفی $\mathbf{n} = (n_0, n_1, n_2)$ است که در آن $n_0 + n_1 + n_2 = N$، با این تفسیر که $n_0$ ماشین در حال کار هستند، $n_1$ در فاز 1 سرویس هستند، و $n_2$ در فاز 2 سرویس هستند.
	% 	\begin{itemize}
	% 		\item[(الف)] نرخ‌های انتقال لحظه‌ای این زنجیره مارکوف زمان پیوسته را ارائه دهید.

	% 		\item[(ب)] با تفسیر زنجیره معکوس به عنوان مدلی از نوع مشابه، با این تفاوت که ماشین‌ها از کار به فاز 2 و سپس به فاز 1 سرویس می‌روند، نرخ‌های انتقال زنجیره معکوس را حدس بزنید. در این کار، مطمئن شوید که حدس شما در نرخ خروج از حالت $(n, k, j)$ در هنگام بازدید برابر با نرخی است که زنجیره رو به جلو از آن حالت خارج می‌شود.

	% 		\item[(ج)] ثابت کنید که حدس شما صحیح است و احتمالات حدی را بیابید.
	% 	\end{itemize}
	% \end{problem}

	% \begin{problem}
	%  یک مدل صف‌بندی ترتیبی با سه \textbf{سرور} را در نظر بگیرید، که مشتریان با یک فرایند پواسون با نرخ $\lambda$ به \textbf{سرور} 1 وارد می‌شوند. پس از اتمام سرویس در \textbf{سرور} 1، مشتری سپس به \textbf{سرور} 2 می‌رود؛ پس از اتمام سرویس در \textbf{سرور} 2، مشتری به \textbf{سرور} 3 می‌رود؛ پس از اتمام سرویس در \textbf{سرور} 3، مشتری سیستم را ترک می‌کند. با فرض اینکه زمان‌های سرویس در \textbf{سرور} $i$ با نرخ $\mu_i$ برای $i=1, 2, 3$ نمایی هستند، احتمالات حدی این سیستم را با حدس زدن زنجیره معکوس و سپس تأیید صحت حدس خود بیابید.		
	% \end{problem}

\end{document}

